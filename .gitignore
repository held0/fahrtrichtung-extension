node_modules/
.superpowers/

# Intern — NICHT ins oeffentliche Extension-Repo (Marketing-Seite + Strategie-Doku)
landing/
docs/

# Interne Arbeitsdateien / lokale Claude-Config
TODO.md
.claude/settings.local.json

# Vorher/Nachher-Reports der Referenztests (Referenzdaten selbst bleiben versioniert)
reference/compare-*.json

# Lokale Secrets (Proxy-Zugang fuer Testharnesse)
.env*
